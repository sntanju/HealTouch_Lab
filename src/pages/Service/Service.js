import React from 'react';
import './Service.css';

const Service = (props) => {
    
    // const {img} = props?.service;

    // console.log(service);
    return (
        <div>
          <h2>service</h2>
        </div>
    );
};

export default Service;